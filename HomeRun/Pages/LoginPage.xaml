<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HomeRun.Pages.LoginPage"
             Shell.NavBarIsVisible="false">
            
    <ContentPage.Content>
        <StackLayout>
            
            <Image Source="APP_HomeRun_Logo.png" />

            <Grid Padding="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
            
            
            <Entry Grid.Row="1" Keyboard="Email" Placeholder="User" x:Name="UserLoginEmail" />
            <Entry Grid.Row="2" IsPassword="True" Placeholder="Password" x:Name="UserLoginPassword" />
            <Button Grid.Row="3" 
                    Text="Login" 
                    TextColor="White"
                    FontSize="Medium"
                    FontAttributes="Bold"
                    BackgroundColor="Orange"                    
                    Clicked="Button_Clicked" />
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>